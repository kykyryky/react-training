{
  "name": "react-todo-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.11.0",
    "pre-push": "^0.1.1",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^16.5.0",
    "react-dom": "^16.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "gh-pages -d build -m \"Deploy gh-pages\"",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "pre-push": ["build", "deploy"],
  "homepage": "/"
}
